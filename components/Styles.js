import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        paddingTop: 40,
        alignItems: 'center',
    },

    header: {
        fontSize: 28,
        fontWeight: 'bold',
        marginBottom: 16,
        color: '#222',
    },

    scroll: {
        width: '100%',
    },

    scrollContent: {
        paddingHorizontal: 16,
        paddingBottom: 24,
    },

    poiName: {
        fontSize: 24,
        fontWeight: 'bold',
        marginTop: 8,
    },
});
